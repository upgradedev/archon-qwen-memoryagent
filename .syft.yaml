# Reviewed, fail-closed production-image inventory policy. Every CI invocation
# passes this path explicitly so Syft cannot auto-discover another repository or
# user configuration.
check-for-app-update: false
scope: squashed
default-catalogers: []
select-catalogers: []
exclude: []
