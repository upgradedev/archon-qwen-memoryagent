# Base URL of the live Archon MemoryAgent HTTP API, used only when the "Live"
# toggle is on. When unset, the app falls back to the default below.
#
# NOTE: the dashboard defaults to DEMO mode (canned data, zero backend). The
# Live toggle attempts real calls to this URL and gracefully falls back to demo
# on any error/timeout. See web/README.md for the mixed-content / CORS notes.
VITE_API_URL=http://43.106.13.19:9000
