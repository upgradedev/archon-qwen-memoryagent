# Sanitized submission gallery

Only final, rights-reviewed, secret-free MemoryAgent screenshots belong here.
Capture names, deterministic order and exact English captions are defined in
[`../SCREENSHOT_MANIFEST.md`](../SCREENSHOT_MANIFEST.md). The fail-closed one-command
builder is documented in [`../MEDIA_CAPTURE_RUNBOOK.md`](../MEDIA_CAPTURE_RUNBOOK.md).
Raw captures must remain under ignored `demo/private-originals/` and must never be
staged.

Every Devpost file produced here is exactly 1500×1000 (3:2). Its no-crop 1920×1080
video equivalent is written under `demo/final-media/proof-frames/` with the same
stem plus `-16x9`. `CAPTURE_REVIEW.json` records exact-release/model gates and the
SHA-256 of every approved artifact.

No screenshot is committed yet; absence is intentional until the verified live
release is captured and every frame passes the security and rights review.
