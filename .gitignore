# dependencies
node_modules/

# build output
dist/

# code-coverage output (c8 / nyc)
coverage/
.nyc_output/

# env / secrets — never commit
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Serverless Devs local state
.s/

# demo-video pipeline artifacts (generated in CI from the live run)
docs/screencast_transcript.txt
demo/video/
*.mp4
*.mp3
# ...except the committed Proof-of-Alibaba-Cloud-Deployment recording (small submission artifact)
!demo/alibaba-proof.mp4
caps/
body.json
# web-segment pipeline scratch (generated in CI from the live Explorer UI)
web_shots/
web_cap.txt
web_concat.txt
narration_main.txt
narration_web.txt
