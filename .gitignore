# dependencies
node_modules/
__pycache__/
*.py[cod]

# build output
dist/

# code-coverage output (c8 / nyc)
coverage/

# readiness gate report (regenerated by `npm run readiness` / CI)
readiness.json
.nyc_output/

# Repo-local private/scratch workspace. Generated tools must not leave persistent
# artifacts in OS temp or outside the project; sanitized finals belong in
# demo/gallery/ and benchmark evidence in bench/results/.
.artifacts/
demo/private-originals/

# env / secrets — never commit
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Serverless Devs local state
.s/

# demo-video pipeline artifacts (generated in CI from the live run)
docs/screencast_transcript.txt
demo/video/
*.mp4
*.mp3
# ...except the final app-specific MemoryAgent runtime proof once sanitized.
!demo/gallery/
!demo/gallery/memoryagent-alibaba-runtime-proof.mp4
caps/
body.json
# web-segment pipeline scratch (generated in CI from the live Explorer UI)
web_shots/
web_cap.txt
web_concat.txt
narration_main.txt
narration_web.txt
